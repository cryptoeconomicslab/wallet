/**
 * color files.
 * mainly, use dark background and light color for text.
 * for inverse use, use light background and dark text color.
 */

const BG_GRADIENT_FROM = '#FF7E58'
const BG_GRADIENT_TO = '#B12289'

const TEXT_MAIN = '#FFFFFF'

export default {
  BG_GRADIENT_TO,
  BG_GRADIENT_FROM,
  TEXT_MAIN
}
